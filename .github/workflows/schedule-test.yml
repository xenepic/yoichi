name: schedule-test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "scheduled test"
          date
          TZ=Asia/Tokyo date
